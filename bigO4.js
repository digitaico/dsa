'use strict';

function compressBoxesTwice(boxes, boxes2) {
  boxes.forEach((boxes) => console.log(boxes)); //O(n))
  boxes2.forEach((boxes) => console.log(boxes)); //O(n)
}
 // different terms for inputs: 2 inputs, a, b
// R BIG O(a + b)
